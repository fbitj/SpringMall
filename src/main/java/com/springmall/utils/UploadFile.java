package com.springmall.utils;

import org.springframework.web.multipart.MultipartFile;

public class UploadFile {
    public static String uploadImg(MultipartFile myfile) {
        String name = myfile.getName();
        return null;
    }
}
